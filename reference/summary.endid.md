# Summarize an endid object

Summarize an endid object

## Usage

``` r
# S3 method for class 'endid'
summary(object, ...)
```

## Arguments

- object:

  An `endid` object.

- ...:

  Ignored.

## Value

A `summary.endid` object (list with att_table and qte_table).
