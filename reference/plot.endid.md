# Plot QTE from an endid object

Produces a quantile treatment effect plot with confidence intervals and
an ATT reference line.

## Usage

``` r
# S3 method for class 'endid'
plot(x, ...)
```

## Arguments

- x:

  An `endid` object.

- ...:

  Ignored.

## Value

A `ggplot` object (invisibly).
