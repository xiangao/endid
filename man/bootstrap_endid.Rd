% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_endid}
\alias{bootstrap_endid}
\title{Bootstrap inference for endid}
\usage{
bootstrap_endid(
  Y,
  D,
  controls = NULL,
  nboot = 200,
  quantiles = seq(0.1, 0.9, 0.1),
  nsample = 500,
  alpha = 0.05,
  noise_dim = 5,
  hidden_dim = 100,
  num_layer = 3,
  num_epochs = 1000,
  lr = 0.001,
  silent = TRUE,
  num_cores = 1
)
}
\arguments{
\item{Y}{Numeric vector of residualized outcomes.}

\item{D}{Binary treatment indicator.}

\item{controls}{Matrix of controls or NULL.}

\item{nboot}{Number of bootstrap replications (default: 200).}

\item{quantiles}{Quantiles for QTE (default: seq(0.1, 0.9, 0.1)).}

\item{nsample}{Monte Carlo samples for engression predict (default: 500).}

\item{alpha}{Significance level for CIs (default: 0.05).}

\item{noise_dim, hidden_dim, num_layer, num_epochs, lr, silent}{Engression parameters.}

\item{num_cores}{Number of cores for parallel execution (default: 1).}
}
\value{
List with: se, ci_lower, ci_upper, att_boot, att_mean,
qte_se, qte_ci_lo, qte_ci_hi, qte_mean, qte_boot_mat.
}
\description{
Resamples units (with replacement) from the cross-section, refits engression,
and computes ATT and QTE on each replicate. Returns SEs and percentile CIs.
Supports parallel execution via mclapply on Unix-like systems.
}
\keyword{internal}
