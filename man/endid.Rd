% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endid.R
\name{endid}
\alias{endid}
\title{Engression-Based Distributional Difference-in-Differences}
\usage{
endid(
  data,
  y,
  ivar,
  tvar,
  gvar = NULL,
  post = NULL,
  dvar = NULL,
  rolling = "demean",
  control_group = "never_treated",
  aggregate = "overall",
  controls = NULL,
  season_var = NULL,
  quantiles = seq(0.1, 0.9, 0.1),
  nsample = 500,
  nboot = 200,
  noise_dim = 5,
  hidden_dim = 100,
  num_layer = 3,
  num_epochs = 1000,
  lr = 0.001,
  silent = TRUE
)
}
\arguments{
\item{data}{A long-format panel data frame.}

\item{y}{Character. Outcome column name.}

\item{ivar}{Character. Unit identifier column name.}

\item{tvar}{Character. Calendar time column name (numeric).}

\item{gvar}{Character or NULL. First-treatment-year column for staggered
designs. Units with value 0, NA, or Inf are never-treated.}

\item{post}{Character or NULL. Binary post-treatment indicator column
(0 = pre, 1 = post). Required when \code{gvar = NULL}.}

\item{dvar}{Character or NULL. Binary treatment group indicator column
(1 = treated unit, 0 = control). Required for common-timing designs when
\code{post} is a calendar indicator (all units observed pre and post).}

\item{rolling}{Character. Transformation method: \code{"demean"} (default),
\code{"detrend"}, \code{"demeanq"}, \code{"detrendq"}.}

\item{control_group}{Character. Control group for staggered designs:
\code{"never_treated"} (default) or \code{"not_yet_treated"}.}

\item{aggregate}{Character. Aggregation for staggered designs:
\code{"overall"} (default), \code{"cohort"}, or \code{"none"}.}

\item{controls}{Character vector or NULL. Time-invariant control column names.}

\item{season_var}{Character or NULL. Seasonal indicator column.}

\item{quantiles}{Numeric vector. Quantiles for QTE (default: seq(0.1, 0.9, 0.1)).}

\item{nsample}{Integer. Monte Carlo samples for engression predictions (default: 500).}

\item{nboot}{Integer. Bootstrap replications for inference (default: 200).}

\item{noise_dim}{Engression noise dimension (default: 5).}

\item{hidden_dim}{Engression hidden layer width (default: 100).}

\item{num_layer}{Engression number of layers (default: 3).}

\item{num_epochs}{Engression training epochs (default: 1000).}

\item{lr}{Engression learning rate (default: 1e-3).}

\item{silent}{Logical. Suppress engression training output (default: TRUE).}
}
\value{
An object of class \code{"endid"}.
}
\description{
Estimates distributional treatment effects by combining Lee & Wooldridge
(2025) panel DiD transformations with engression distributional regression.
Produces ATT, quantile treatment effects (QTE), and counterfactual
distributions.
}
\examples{
\donttest{
  castle <- read.csv(system.file("extdata", "castle.csv", package = "endid"))
  castle$gvar <- castle$effyear
  castle$gvar[is.na(castle$gvar) | castle$gvar == 0] <- NA
  res <- endid(castle, "lhomicide", "sid", "year", gvar = "gvar",
               rolling = "demean", num_epochs = 500, nboot = 50)
  print(res)
}

}
